<div class="card">
    <div class="card-block">
        <h3 class="card-title">{{ entry.created_at|date('M jS, Y g:i a', timezone="America/Chicago") }}</a></h3>
        <h5 class="card-subtitle">Last Updated at {{ entry.updated_at|date('M jS, Y g:i a', timezone="America/Chicago") }}</a></h5>
        <p class="card-text">{{ entry.body|slice(0,144) }}...</p>
        <a href="{{ url_route('journalentries.show', { 'id': entry.id }) }}" class="card-link">Read More</a>
    </div>
</div>
