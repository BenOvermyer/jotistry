{% extends 'layout' %}

{% block content %}
    <div class="row">
        <div class="col">
            <a href="{{ url_route('journalentries.create') }}" class="btn btn-primary"><i class="material-icons">add_circle</i> New Entry</a>
        </div>
    </div>
    <div class="row">
        <div class="col">
            {% for entry in entries %}
                {% include 'journal.card' %}
            {% endfor %}
        </div>
    </div>
{% endblock %}
